---
"@deprecation-lens/cli": minor
---

Support filtering deprecation results by message content via `--msg-grep` option. Use `--msg-grep-case-sensitive` for case-sensitive matching and `--msg-grep-regex` to treat patterns as regular expressions.
