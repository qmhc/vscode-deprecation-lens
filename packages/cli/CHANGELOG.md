# @deprecation-lens/cli

## 1.1.0

### Minor Changes

- ad25f2e: Support filtering deprecation results by message content via `--msg-grep` option. Use `--msg-grep-case-sensitive` for case-sensitive matching and `--msg-grep-regex` to treat patterns as regular expressions.

## 1.0.0

### Major Changes

- f7a58da: CLI init.
